/* 계정 동기화 설정
 *
 * 비어 있으면 동기화 기능이 화면에 나타나지 않고, 앱은 지금까지처럼
 * 폰 안에만 기록을 저장한다.
 *
 * anonKey 는 공개되어도 되는 값이다. 데이터 보호는 이 키가 아니라
 * 데이터베이스의 행 수준 보안(RLS)이 담당한다 — 로그인한 본인의 행만
 * 읽고 쓸 수 있다.
 */
window.DSM = window.DSM || {};

DSM.SupabaseConfig = {
  url: '',
  anonKey: ''
};
